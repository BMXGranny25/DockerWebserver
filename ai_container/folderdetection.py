# always run and detect when png image is added to folder

# detection folder is TempImage

# when image is detected run ai

# wait for ai results to be done

# delete image
import os
import time

while (True):
    if ():
        print("Image")
    else:
        print("No Image")
    time.sleep(5)
